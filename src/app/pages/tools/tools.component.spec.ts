import { ToolsComponent } from './tools.component';

describe('ToolsComponent', () => {
  let component: ToolsComponent;

  beforeEach(() => {
    component = new ToolsComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
